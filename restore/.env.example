# REQUIRED: Project name (must match the backup archive prefix)
PROJECT_NAME=myapp

# REQUIRED: S3 Configuration
S3_BUCKET_URL=s3://backup-test
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_DEFAULT_REGION=us-east-1

# OPTIONAL: For S3-compatible storage like MinIO
# AWS_ENDPOINT_URL=http://localhost:3900

# OPTIONAL: Specific backup file to restore (if not set, uses latest)
# BACKUP_FILE=backup_myapp_20231105-120000-uuid.tar.zst

# OPTIONAL: Restore mode (latest, specific, list)
# RESTORE_MODE=latest

# OPTIONAL: Skip stopping the stack (default: false)
# SKIP_STOP=false

# OPTIONAL: Skip starting the stack after restore (default: false)
# SKIP_START=false
